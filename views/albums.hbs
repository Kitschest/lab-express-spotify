{{#each albums}}
<div class="card mb-3">
  <img src="{{images.[0].url}}" alt="{{name}} album cover" class="card-img-top">
  <div class="card-body">
    <h5 class="card-title">{{name}}</h5>
    <a href="/tracks/{{id}}" class="btn btn-success">View Tracks</a>
  </div>
</div>
{{/each}}

